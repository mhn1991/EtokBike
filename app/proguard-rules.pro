# Project-specific R8 rules.
#
# The app currently uses only Android framework classes and org.json, so the
# default Android optimized rules are enough. This file exists because the
# release build references it from app/build.gradle.
